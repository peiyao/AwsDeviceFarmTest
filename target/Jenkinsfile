pipeline {
 
 stages {
         stage('RUNNING') 
         {
             steps 
             {
                sh 'package -DskipTests'
             }
         }
         stage('Build') 
         {
             steps {
               sh 'echo hello'
               sh 'just for test'
             }
          }
 }
}

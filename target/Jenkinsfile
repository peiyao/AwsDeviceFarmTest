pipeline {
 /*agent {
   label 'kubernetes'
 }*/
/*
 options {
   ansiColor('xterm')
   buildDiscarder(logRotator(numToKeepStr: '10', daysToKeepStr: '10'))
 }*
/

 stages {
  stage('CheckOut') {
  steps {
   checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: '0b51730e-cdcf-4d2d-bdb6-b8c2af96b79e', url: 'https://github.com/peiyao/AwsDeviceFarmTest.git']]])
   sh 'Check out done'} }
  stage('Build') {
     steps {
       sh 'echo hello'
       sh 'just for test'
     }
   }
 }
}
